name: swaju_ide
description: "./ACE - Local AI Compiler Engine"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.7.0
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.4.2
  re_editor: ^0.10.0
  re_highlight: ^0.0.3
  path_provider: ^2.1.5
  lib_llama_cpp: ^0.7.3
  google_fonts: ^6.2.1
  flutter_svg: ^2.3.0
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
